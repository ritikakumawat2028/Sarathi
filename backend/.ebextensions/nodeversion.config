# Node.js version is set via .node-version file in the project root.
# The NodeVersion option_setting is NOT supported on Amazon Linux 2 / AL2023 platforms.
# Do not use: option_settings > aws:elasticbeanstalk:container:nodejs > NodeVersion